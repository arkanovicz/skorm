package $package

/**
 * This file has been generated by the skorm gradle plugin.
 *
 * Two halves: the read-only database, its schemas and row interfaces, and the mutable database
 * extending it, whose row interfaces add the writes. A row interface carries every accessor; the
 * class behind it (`XImpl`) is the storage, plus the blocking twins reflection-driven callers reach.
 */

#if($useJsonType || !$resolved.attributes.isEmpty())
import com.republicate.kson.Json
#end
#if($useUuidType)
import kotlin.uuid.Uuid
#end
#if($useDatetimeType || !$resolved.attributes.isEmpty())
import kotlinx.datetime.*
#end
import com.republicate.skorm.*
import kotlinx.coroutines.flow.*
import kotlin.jvm.JvmName
#foreach ($schema in $resolved.schemas)
#foreach ($enum in $schema.enums)
import ${package}.${resolved.databaseClass}.${schema.className}.${enum.name}
#end
#end

## ---- macros: what both halves print the same way -------------------------------------------------
#macro(fieldGetter $field)#if($field.enumClass)${field.getter}("$field.name")#if($field.nullable)?#{else}!!#end.let { ${field.enumClass}.valueOf(it) }#{else}${field.getter}("$field.name")#if(!$field.nullable)!!#end#end#end
#macro(fieldSetter $field)#if($field.enumClass)put("$field.name", v#if($field.nullable)?#end.toString())#{else}put("$field.name", v)#end#end
#macro(accessorArgs $attr)#if($attr.arguments), #if($attr.arguments.size() > 1)#namedValues($attr.arguments)#else#values($attr.arguments)#end#end#end
#macro(entityCompanion $half $schema $entity)
            companion object: Entity("$entity.objectName", ${half.databaseClass}.${schema.objectName}#if($entity.parentClass), ${entity.parentClass}#end)#if($half.mutable), MutableEntity#end {
                override fun new(): ${entity.className} = ${entity.implClass}(this)
#if (!$entity.kinds.isEmpty())
                // rows are read joined with the subtypes' tables: each comes back as the class its kind names
                override val source = "$entity.source"
                override fun new(kind: String?): ${entity.className} = when (kind) {
                    "$entity.tableName" -> new()
#foreach ($kind in $entity.kinds)
                    "$kind.first" -> ${kind.second}.new()
#end
                    else -> throw SkormException("$entity.tableName rows carry no kind: a hierarchy is read with its subtypes joined")
                }
#end
#if ($entity.hasPrimaryKey)
                @Suppress("UNCHECKED_CAST")
                override suspend fun fetch(vararg key: Any) = super.fetch(*key) as ${entity.className}?
#end
                @Suppress("UNCHECKED_CAST")
                override suspend fun browse() = super.browse() as Flow<${entity.className}>
                fun initialize() {
                    if (fields.isNotEmpty()) return // the mutable database initializes its read-only sibling too
#foreach ($field in $entity.fields)
                    addField(Field("$field.name", "$field.rawType", $field.primaryKey, $field.generated))
#end
                }
            }
#end
#macro(blockingTwins $entity)
#foreach ($join in $entity.joins)
            // the blocking twin of `${join.name}()`: what a reflection-driven caller reaches under that name
            @JvmName("$join.name")
            fun ${join.name}Blocking(): #if($join.multiple)List<$join.targetClass>#{else}$join.returnType#end = blocking { ${join.identifier}()#if($join.multiple).toList()#end }
#end
#foreach ($attr in $entity.attributes)
#if($attr.verb != "perform")
            @JvmName("$attr.name")
            fun ${attr.name}Blocking(#typedArguments($attr.arguments)) = blocking { `${attr.name}`(#values($attr.arguments))#if($attr.verb == "query").toList()#end }
#end
#end
#end
## ---- one half: the database, its schemas, their row interfaces and impl classes ------------------
#macro(half $half)
#if($useUuidType)
@OptIn(kotlin.uuid.ExperimentalUuidApi::class)
#end
#if($half.mutable)
class ${half.databaseClass}(processor: Processor): ${half.base}(processor), MutableDatabase {
#else
open class ${half.databaseClass}(processor: Processor): Database("$half.name", processor) {
#end
    companion object {
        lateinit var instance: ${half.databaseClass}
        fun isInstantiated() = this::instance.isInitialized
#foreach ($schema in $half.schemas)
        val ${schema.objectName}: ${schema.className} get() = instance.${schema.objectName}
#end
    }
    init {
#if($half.mutable)
        if (isInstantiated()) throw RuntimeException("${half.databaseClass} instance already created")
        instance = this
#elseif($half.mutableTwin)
        // the mutable database claims its own singleton; this one is the read-only database, or its read-only sibling
        if (this !is ${half.mutableTwin.databaseClass}) {
            if (isInstantiated()) throw RuntimeException("${half.databaseClass} instance already created")
            instance = this
        }
#else
        if (isInstantiated()) throw RuntimeException("${half.databaseClass} instance already created")
        instance = this
#end
    }
#if($half.mutable)
    /** the read-only database over the same processor: what a render that must not write is handed */
    override val readOnly: ${half.base} = ${half.base}(processor)
#end
#if($half.mutable)#set($modifier = "override")#{else}#set($modifier = "open")#end
#foreach ($schema in $half.schemas)
    $modifier val ${schema.objectName}: ${schema.className} by lazy { ${schema.className}(this) }
#end
    override fun initialize() {
#foreach ($schema in $half.schemas)
        ${schema.objectName} // the schema exists on this instance before the entities register
#end
#foreach ($schema in $half.schemas)
#foreach ($entity in $schema.entities)
        ${schema.className}.${entity.className}.initialize()
#end
#end
        super.initialize()
#if($half.mutable)
        readOnly.initialize()
#elseif($half.mutableTwin)
        if (this !is ${half.mutableTwin.databaseClass}) registerAttributes() // once per store, after both databases exist
#else
        registerAttributes()
#end
    }
#foreach ($schema in $half.schemas)
#if($half.mutable)
    class ${schema.className}(db: Database): ${schema.base}(db), MutableSchema {
#else
    open class ${schema.className}(db: Database): Schema("$schema.name", db) {
#end
#if(!$half.mutable)
#foreach ($enum in $schema.enums)
        enum class $enum.name {
#foreach ($value in $enum.values)
            $value#if ($foreach.hasNext),#end
#end
        }
#end
#end
#foreach ($entity in $schema.entities)
## the row interface: every field and accessor; the mutable one adds the setters and the mutations
        interface ${entity.className} : #if($half.mutable)${entity.base}, MutableInstance#{else}Instance#end#if($entity.parentClass), ${entity.parentClass}#end {
#entityCompanion($half $schema $entity)
#foreach ($field in $entity.ownFields)
#if(!$half.mutable)
            val ${field.identifier}: ${field.kotlinType}#if($field.nullable)?#end
                get() = #fieldGetter($field)
#elseif($field.writable)
            override var ${field.identifier}: ${field.kotlinType}#if($field.nullable)?#end
                get() = #fieldGetter($field)
                set(v) { #fieldSetter($field) }
#end
#end
#if($half.mutable)#set($override = "override ")#{else}#set($override = "")#end
#foreach ($join in $entity.joins)
            // $join.label
            ${override}suspend fun ${join.identifier}(): $join.returnType = ${join.verb}("$join.name")
#end
#foreach ($attr in $entity.attributes)
#if(!$half.mutable || $attr.entityRows || $attr.verb == "perform")
#if($half.mutable && $attr.entityRows)#set($override = "override ")#{else}#set($override = "")#end
            // attribute $attr.qualifiedName
            ${override}suspend fun `${attr.name}`(#typedArguments($attr.arguments)) = ${attr.verb}${attr.generics}("$attr.name"#accessorArgs($attr))$attr.cast
#end
#end
        }
## the class behind it: the storage, and the blocking twins (final members, hence not in the interface)
        open class ${entity.implClass}(entity: Entity = ${entity.className}): #if($entity.parentImplClass)${entity.parentImplClass}(entity)#{else}#if($half.mutable)MutableInstanceImpl#{else}InstanceImpl#end(entity)#end, ${entity.className} {
#if ($half.mutable && $entity.kindValue)
            // a new row knows its kind before the database applies it; a subtype's init runs last and wins
            init { put("kind", "$entity.kindValue") }
#end
#blockingTwins($entity)
        }
#end
#foreach ($attr in $schema.attributes)
#if(!$half.mutable || $attr.entityRows || $attr.verb == "perform")
#if($half.mutable && $attr.entityRows)#set($override = "override ")#{else}#set($override = "")#end
        // attribute $attr.qualifiedName
        ${override}suspend fun `${attr.name}`(#typedArguments($attr.arguments)) = ${attr.verb}${attr.generics}("$attr.name"#accessorArgs($attr))$attr.cast
#end
#if(!$half.mutable && $attr.verb != "perform")
        @JvmName("$attr.name")
        fun ${attr.name}Blocking(#typedArguments($attr.arguments)) = blocking { `${attr.name}`(#values($attr.arguments))#if($attr.verb == "query").toList()#end }
#end
#end
    }
#end
}
#end
## ---- the file --------------------------------------------------------------------------------------
#if($multiplatform)
/** The platform's registrations of the navigations and ksql attributes, run by initialize(). */
internal expect fun ${resolved.databaseClass}.registerAttributes()

#end
#half($resolved)
#if($resolved.mutableTwin)

#half($resolved.mutableTwin)
#end
