package $package

/**
 * This file has been generated by the skorm gradle plugin.
 */

#if($useJsonType)
import com.republicate.kson.Json
#end
#if($useUuidType)
import kotlin.uuid.Uuid
#end
#if($useDatetimeType)
import kotlinx.datetime.*
#end
import com.republicate.skorm.*

#if($useUuidType)
@OptIn(kotlin.uuid.ExperimentalUuidApi::class)
#end
class ${resolved.databaseClass}(processor: Processor): Database("$resolved.name", processor) {
    companion object {
        lateinit var instance: Database
        private fun isInstantiated() = this::instance.isInitialized
#foreach ($schema in $resolved.schemas)
        val ${schema.objectName}: ${schema.className} by lazy { ${schema.className}(instance) }
#end
    }
    init {
        if (isInstantiated()) throw RuntimeException("${resolved.databaseClass} instance already created")
        instance = this
    }
    override fun initialize() {
#foreach ($schema in $resolved.schemas)
#foreach ($entity in $schema.entities)
${schema.className}.${entity.className}.initialize()
#end
#end
        super.initialize()
    }
#foreach ($schema in $resolved.schemas)
    class ${schema.className}(db: Database): Schema("$schema.name", db) {
#foreach ($enum in $schema.enums)
        enum class $enum.name {
#foreach ($value in $enum.values)
            $value#if ($foreach.hasNext),#end
#end
        }
#end
#foreach ($entity in $schema.entities)
## the field contract, so a non-suspend view of a row can delegate it
        interface ${entity.className}Fields#if($entity.parentClass) : ${entity.parentClass}Fields#end {
#foreach ($field in $entity.ownFields)
            val ${field.identifier}: ${field.kotlinType}#if($field.nullable)?#end
#end
        }
        open class ${entity.className}(entity: Entity = Companion): #if($entity.parentClass)${entity.parentClass}(entity)#{else}Instance(entity)#end, ${entity.className}Fields {
            companion object: Entity("$entity.objectName", $schema.objectName#if($entity.parentClass), ${entity.parentClass}#end) {
                override fun new(): ${entity.className} = ${entity.className}()
#if (!$entity.kinds.isEmpty())
                // rows are read joined with the subtypes' tables: each comes back as the class its kind names
                override val source = "$entity.source"
                override fun new(kind: String?): ${entity.className} = when (kind) {
                    "$entity.tableName" -> new()
#foreach ($kind in $entity.kinds)
                    "$kind.first" -> ${kind.second}.new()
#end
                    else -> throw SkormException("$entity.tableName rows carry no kind: a hierarchy is read with its subtypes joined")
                }
#end
#if ($entity.hasPrimaryKey)
                @Suppress("UNCHECKED_CAST")
                override suspend fun fetch(vararg key: Any) = super.fetch(*key) as ${entity.className}?
#end
                @Suppress("UNCHECKED_CAST")
                override suspend fun browse() = super.browse() as Sequence<${entity.className}>
                @Suppress("UNCHECKED_CAST")
                override suspend operator fun iterator() = super.iterator() as Iterator<${entity.className}>
                fun initialize() {
#foreach ($field in $entity.fields)
                    addField(Field("$field.name", "$field.rawType", $field.primaryKey, $field.generated))
#end
                }
            }
#foreach ($field in $entity.ownFields)
## TODO output PKs first
## TODO use delegates
            override #if($field.writable)var#{else}val#end ${field.identifier}: ${field.kotlinType}#if($field.nullable)?#end
#if($field.enumClass)
                get() = ${field.getter}("$field.name")#if($field.nullable)?#{else}!!#end.let { ${field.enumClass}.valueOf(it) }
#if($field.writable)
                set(v) { put("$field.name", v#if($field.nullable)?#end.toString()) }
#end
#else
                get() = ${field.getter}("$field.name")#if(!$field.nullable)!!#end
#if($field.writable)
                set(v) { put("$field.name", v) }
#end
#end
#end
        }
#end
    }
#end
}

